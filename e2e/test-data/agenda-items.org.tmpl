# Template for agenda-items.org — rendered by e2e/generate-test-data.js.
# The d:OFFSET placeholders (optionally with HH:MM) expand to org timestamp
# bodies OFFSET days from today (see e2e/test-dates.js). Do not edit the
# generated .org file.
#
# Date layout the e2e tests rely on (agenda.test.ts, scheduling.test.ts):
#   yesterday: "Morning standup", "Code review"
#   today:     "Team meeting", "Submit report" (deadline), "Doctor appointment"
#   tomorrow:  "Review code", "Update dependencies"
#+TITLE: Agenda Items

* Calendar
** TODO Morning standup
   SCHEDULED: <{{d:-1 09:00}}>
   Daily standup meeting

** TODO [#A] Team meeting
   SCHEDULED: <{{d:0 10:00}}>
   Weekly standup

** TODO Submit report
   DEADLINE: <{{d:0}}>
   Quarterly report due

** TODO Doctor appointment
   SCHEDULED: <{{d:0 14:30}}>

* Weekly Tasks
** TODO Code review
   SCHEDULED: <{{d:-1}}>
   Review pending PRs

** TODO Review code
   SCHEDULED: <{{d:+1}}>

** TODO Update dependencies
   SCHEDULED: <{{d:+1}}>

* Next Action Items
** NEXT Prepare demo for stakeholders
   Demo the new features

** NEXT Review pull request from team
   :PROPERTIES:
   :ID: next-pr-review
   :END:

** NEXT [#A] Fix critical bug :work:
   High priority next action

* In Progress
** STARTED Writing documentation :work:
   Currently working on API docs

** STARTED Refactoring auth module
   Making the code cleaner

* Blocked
** WAITING Client approval for design
   Need sign-off before proceeding

** WAITING Response from vendor
   Waiting on pricing info

* Work Projects
** TODO [#A] Deploy to production :work:
   Critical deployment task

** TODO Backend API improvements :work:
   Refactor endpoints
